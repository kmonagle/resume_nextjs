// Why this file exists: Next.js configuration. Intentionally empty: the app runs
// on defaults, and nothing here opts in to server-side caching.
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
